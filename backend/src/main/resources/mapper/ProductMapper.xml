<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tim.document.web1.mapper.ProductMyBatisMapper">

    <select id="findAll"
        resultType="com.tim.document.web1.model.Product">

        SELECT
            id,
            name,
            category,
            price,
            stock
        FROM products

    </select>


    <select id="findPage"
        resultType="com.tim.document.web1.model.Product">

        SELECT
            id,
            name,
            category,
            price,
            stock
        FROM products
        ORDER BY id
        OFFSET #{offset} ROWS
    	FETCH NEXT #{size} ROWS ONLY
        <!-- LIMIT #{offset}, #{size} -->

    </select>
    
	<select id="findById"
			parameterType="java.lang.Integer"
			resultType="com.tim.document.web1.model.Product">
		SELECT
			id,
			name,
			category,
			price,
			stock
		FROM products
		WHERE id=#{id}
	</select>
	
	<update id="updateStock">
		UPDATE products
		SET stock = #{stock}
		WHERE id=#{id}
	</update>
	
	<update id="decreaseStock">
		UPDATE products
		SET stock=stock-#{quantity}
		WHERE id=${id}
			AND stock>=#{quantity}
	</update>
</mapper>